(() => {
  class AboutPageComponent extends Component {
    containerEl = null;

    constructor(props) {
      super(props);
    }

    render() {
      // $(".about").html(`
      //     <p>Ido BZ</p>
      // `);
    }
  }

  const ap = new AboutPageComponent().render();
})();
